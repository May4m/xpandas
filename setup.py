from setuptools import find_packages, setup


setup(
    name="xpandas",
    version="0.1.0",
    description="Experimental object-storage-backed queue primitives for XPandas.",
    packages=find_packages(),
)
